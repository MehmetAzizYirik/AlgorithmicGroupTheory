package Orbits;

import java.util.HashSet;
import java.util.Set;

public class Orbit {
	 public static int i, deg;
	 public static int size=4;
	 public static Set<Integer> newSet() {
		  Set<Integer> set = new HashSet<Integer>();
		  S->V=(UINT *)calloc(S->U->WORDS,sizeof(UINT));
		  return(set);
	 }
}
